#!/usr/bin/env node
// Published entry point. The CLI itself is TypeScript compiled to dist/ by `npm run build`,
// so this stays a shim rather than duplicating any argument handling.
import "../dist/cli.js";
